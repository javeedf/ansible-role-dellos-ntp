---
# vars file for Dell-Networking.dellos-ntp, below gives a sample configuration
# Sample variables for dellos9 device
dellos_ntp:
    server:
      - ip: 2.2.2.2
        vrf:
          - test
          - tes
        state: present
